import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "Monopoly Banker",
  appName: "com.dushin.monopolybanker",
  webDir: "dist",
};

export default config;
